
import React, { useEffect, useRef } from 'react';
import CircleCanvas from '@/components/CircleCanvas';

const Index = () => {
  return (
    <div className="min-h-screen w-full overflow-hidden bg-gradient-to-br from-purple-50 to-indigo-100">
      <CircleCanvas />
    </div>
  );
};

export default Index;
